# Production Environment Configuration for Vue Client (Vite)
# Place this file in client/.env.production
# This file is used when building for production: npm run build

# API Base URL for production (same domain, relative path)
# IMPORTANT: Use relative path so API calls go to same domain
VITE_API_BASE=/api

# Environment
NODE_ENV=production
