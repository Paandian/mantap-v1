# Production Environment Configuration
# Copy this to server/.env on your production server
# ⚠️  CRITICAL: Change all default values before deploying!

# Database Configuration
DB_HOST=localhost
DB_USER=your_db_username
DB_PASSWORD=your_secure_password
DB_NAME=mantap_work_db

# Application Settings
NODE_ENV=production
PORT=5000

# Security (MUST be changed!)
JWT_SECRET=your_very_secure_random_jwt_secret_key_here

# CORS (your production domain)
CORS_ORIGIN=https://your-domain.com
